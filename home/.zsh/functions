# Minor stuff. But it's faster to type than `~`.
ch () {
	cd ~/$1
}

code () {
	cd ~/Dropbox/code/$1
}

tmp () {
	local dir=~/tmp/$1
	mkdir -p $dir
	cd $dir
}

# mv current
mvc () {
	local b=$(basename $(pwd))
	cd ..
	mv $b $1
	cd $1
}
