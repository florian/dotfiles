#     a
alias b=brew
alias c=component
#     d
#     e: power alias - editor
#     f
#     g: power alias - git
alias h=heroku
alias i="brew install"
alias j=jobs
alias k="killall -9"
alias l="ls -Gp"
alias m=make
alias n=npm
#     o: power alias - open
#     p
#     q
alias r=rake
alias s=sudo
alias t="tmux new-session -d \; split-window -h \; select-pane -L \; attach"
#     u
alias v=nvim
alias watch="watch \"clear;\"" # Fix for TJ's watch
alias w=watch
#     x
#     z: directory jumper

alias ci="component install"

alias ni="npm install"
alias nig="npm install -g"
alias nis="npm install --save"
alias nisd="npm install --save-dev"
alias ns="npm search"

alias gi="gem install"
alias gs="gem search"

alias bi="brew install"
alias bu="brew upgrade"
alias bs="brew search"
alias binfo="brew info"

alias boi="bower install"
alias bos="bower search"

alias cai="brew cask install"
alias cas="brew cask search"

alias bu=bundle
alias cp="cp -r"
alias ea="subl . &"
alias gr="git repl"
alias mk="mkdir -p"
alias sn="sudo npm"
alias rs="rspec spec"

alias lai="sudo tlmgr install" # Install LaTeX package
alias lawatch="latexmk -pvc -pdf"

alias sudo="sudo " # Make aliases available to sudo

alias cmon="sudo !!"
alias please="sudo !!"

alias cat=vimcat

alias reload!=". ~/.zshrc"
alias r!=reload!

alias tm=tmux
alias tv="tmux new-session -d \; split-window -v \; select-pane -U \; attach"
alias tl="tmux list-sessions"
alias ts="tmux switch -t"
alias tfix="tmux detach -a"
